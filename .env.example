# ── Frontend (Vite — exposed to browser, safe for anon key only) ──
VITE_SUPABASE_URL=https://kzdjowcfgalrijfoxsmi.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ── Server only (Vercel → Settings → Environment Variables) ──
# NEVER prefix with VITE_. Do NOT commit real values.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
ADMIN_PASSWORD=your_strong_admin_password

# Optional alias for API routes (falls back to VITE_SUPABASE_URL)
# SUPABASE_URL=
